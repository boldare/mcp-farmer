name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      publish_only:
        description: "Skip release-please and publish current version to npm"
        required: false
        default: false
        type: boolean

env:
  HUSKY: 0

permissions:
  contents: write
  pull-requests: write
  id-token: write # For npm provenance

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        if: ${{ !inputs.publish_only }}
        with:
          release-type: node

      - uses: actions/checkout@v6
        if: ${{ steps.release.outputs.release_created || inputs.publish_only }}

      - uses: oven-sh/setup-bun@v2
        if: ${{ steps.release.outputs.release_created || inputs.publish_only }}

      - name: Install dependencies
        if: ${{ steps.release.outputs.release_created || inputs.publish_only }}
        run: bun install

      - name: Build
        if: ${{ steps.release.outputs.release_created || inputs.publish_only }}
        run: bun run build

      - uses: actions/setup-node@v6
        if: ${{ steps.release.outputs.release_created || inputs.publish_only }}
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Publish to npm
        if: ${{ steps.release.outputs.release_created || inputs.publish_only }}
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
